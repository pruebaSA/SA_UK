namespace System.Web
{
    using System;

    internal enum StateServicePerfCounter
    {
        STATE_SERVICE_SESSIONS_ABANDONED = 0x58,
        STATE_SERVICE_SESSIONS_ACTIVE = 0x57,
        STATE_SERVICE_SESSIONS_TIMED_OUT = 0x59,
        STATE_SERVICE_SESSIONS_TOTAL = 90
    }
}

